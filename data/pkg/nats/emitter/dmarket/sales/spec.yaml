openapi: "3.0.3"
info:
  title: DMarket Sales Event
  version: "1.0.0"
components:
  schemas:
    SalesEvent:
      type: object
      required:
        - item
        - source
        - event_timestamp
      properties:
        item:
          $ref: "../../models.yaml#/components/schemas/Sale"
        source:
          type: string
          description: Emitter source identifier
        event_timestamp:
          type: string
          format: date-time
          description: When the event was emitted
