openapi: "3.0.3"
info:
  title: DMarket New Listing Event
  version: "1.0.0"
components:
  schemas:
    NewListingEvent:
      type: object
      required:
        - item
        - source
        - event_timestamp
      properties:
        item:
          $ref: "../../models.yaml#/components/schemas/Item"
        source:
          type: string
          description: Emitter source identifier
        event_timestamp:
          type: string
          format: date-time
          description: When the event was emitted
